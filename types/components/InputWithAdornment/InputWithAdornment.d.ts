import React from "react";
import ".style.scss";
export interface Props {
    isError: boolean;
}
declare const InputWithAdornment: ({ isError }: Props) => React.JSX.Element;
export default InputWithAdornment;
